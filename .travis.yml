language: node_js
node_js:
  - "8"

sudo: false

branches:
  only:
    - master

before_install:
  - npm install -g surge

script:
  - echo "Subiendo..."

after_success:
  - ls -l
  - surge -p ./build --domain dev.ideenegocios.com.ar
